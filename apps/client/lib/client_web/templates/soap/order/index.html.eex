<div class="m-4">
  <div class="flex align-center text-xl">
    <div class="mr-2 text-gray-500">
      <%= link("Soap", to: soap_soap_path(@conn, :index)) %>
    </div>
    >
    <div class="ml-2">Orders</div>
  </div>


  <div class="flex flex-col mb-4">
    <%= for order <- @orders do %>
      <div class="mt-5 mb-5">
        <%= link(
          order.name,
          to: soap_order_path(@conn, :show, order.id),
          data: [role: "soap-order-name"]
        ) %>
      </div>
    <% end %>
  </div>

  <%= link(
    "Create order",
    to: soap_order_path(@conn, :new),
    class: "mt-4 button",
    data: [role: "create-soap-order"]
  ) %>
</div>
